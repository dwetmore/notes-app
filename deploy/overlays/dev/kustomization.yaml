apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patchesStrategicMerge:
  - ingress-host.yaml
  - ingress-routes.yaml
  - deployment-image.yaml
  - deployment-env.yaml
  - networkpolicy-ingress-namespace.yaml
# Optional: enable ServiceMonitor only when monitoring.coreos.com CRDs are installed.
# components:
#   - ../../components/monitoring
